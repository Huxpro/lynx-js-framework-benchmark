diff --git a/benchmarks/lynx-table/app/src/App.lynx.tsrx b/benchmarks/lynx-table/app/src/App.lynx.tsrx
index 51971542..11e9eb30 100644
--- a/benchmarks/lynx-table/app/src/App.lynx.tsrx
+++ b/benchmarks/lynx-table/app/src/App.lynx.tsrx
@@ -30,3 +30,4 @@ function countRowRender(): void {
-// Each tick runs in its own macrotask (MessageChannel avoids the nested
-// setTimeout 4ms clamp) so every mutation goes through a full render cycle
-// instead of batching. Mirrors the Vue and React apps.
+// Each tick runs in its own task so every mutation goes through a full render
+// cycle instead of batching. Lynx for Web provides MessageChannel; the Native
+// Engine does not, so Native falls back to its timer task queue. Mirrors the
+// Vue and React apps.
@@ -36,6 +37,29 @@ const STORM_SELECT_TICKS = 30;
-const _stormChannel = new MessageChannel();
-let _stormPending: (() => void) | null = null;
-_stormChannel.port1.onmessage = () => {
-	const cb = _stormPending;
-	_stormPending = null;
-	if (cb) cb();
+interface BenchmarkSnapshot {
+	readonly rowCount: number;
+	readonly firstId: number | null;
+	readonly secondId: number | null;
+	readonly thirdId: number | null;
+	readonly row998Id: number | null;
+	readonly firstLabel: string | null;
+	readonly selectedId: number | null;
+}
+
+interface NativeBenchmarkResult {
+	readonly name: string;
+	readonly source: 'native-tap' | 'devtool-driver';
+	readonly boundary:
+		| 'native-input-handler-to-second-native-frame'
+		| 'native-devtool-driver-handler-to-second-native-frame';
+	readonly commitGate: 'octane-root.flushTransport';
+	readonly startMs: number;
+	readonly commitAckMs: number;
+	readonly firstFrameMs: number;
+	readonly endMs: number;
+	readonly latencyMs: number;
+	readonly snapshot: BenchmarkSnapshot;
+}
+
+type BenchmarkGlobal = typeof globalThis & {
+	__LYNX_BENCH_FLUSH__?: () => Promise<void>;
+	__LYNX_BENCH_SNAPSHOT__?: () => BenchmarkSnapshot;
+	__LYNX_BENCH_ERROR__?: string;
@@ -43,3 +67,67 @@ _stormChannel.port1.onmessage = () => {
-function nextMacrotask(cb: () => void) {
-	_stormPending = cb;
-	_stormChannel.port2.postMessage(0);
+
+function nextNativeFrame(): Promise<number> {
+	return new Promise((resolve) => lynx.requestAnimationFrame(() => resolve(Date.now())));
+}
+
+function reportNativeFailure(error: unknown): never {
+	const message = error instanceof Error ? error.stack ?? error.message : String(error);
+	(globalThis as BenchmarkGlobal).__LYNX_BENCH_ERROR__ = message;
+	console.log('__NATIVE_BENCH_ERROR__', message);
+	throw error;
+}
+
+function measureNative(
+	name: string,
+	action: () => void | Promise<void>,
+	source: NativeBenchmarkResult['source'],
+): Promise<NativeBenchmarkResult | undefined> {
+	const benchmark = globalThis as BenchmarkGlobal;
+	const flush = benchmark.__LYNX_BENCH_FLUSH__;
+	const startMs = Date.now();
+	let actionResult: void | Promise<void>;
+	try {
+		actionResult = action();
+	} catch (error) {
+		return Promise.reject(error).catch(reportNativeFailure);
+	}
+	return Promise.resolve(actionResult)
+		.then(() => {
+			// The deterministic in-process workload renders App through its own root
+			// and intentionally installs no wall-clock instrumentation. Production
+			// benchmark bundles publish the exact root.flushTransport() below.
+			if (flush === undefined) return false;
+			return flush().then(() => true);
+		})
+		.then((instrumented) => {
+			if (!instrumented) return undefined;
+			const commitAckMs = Date.now();
+			return nextNativeFrame().then((firstFrameMs) =>
+				nextNativeFrame().then((endMs) => {
+					const result: NativeBenchmarkResult = {
+						name,
+						source,
+						boundary: source === 'native-tap'
+							? 'native-input-handler-to-second-native-frame'
+							: 'native-devtool-driver-handler-to-second-native-frame',
+						commitGate: 'octane-root.flushTransport',
+						startMs,
+						commitAckMs,
+						firstFrameMs,
+						endMs,
+						latencyMs: endMs - startMs,
+						snapshot: benchmark.__LYNX_BENCH_SNAPSHOT__?.() ?? {
+							rowCount: -1,
+							firstId: null,
+							secondId: null,
+							thirdId: null,
+							row998Id: null,
+							firstLabel: null,
+							selectedId: null,
+						},
+					};
+					console.log('__NATIVE_BENCH_RESULT__', JSON.stringify(result));
+					return result;
+				}),
+			);
+		})
+		.catch(reportNativeFailure);
@@ -48,6 +136,11 @@ function nextMacrotask(cb: () => void) {
-function runStorm(ticks: number, step: (t: number) => void) {
-	let t = 0;
-	const tick = () => {
-		t += 1;
-		step(t);
-		if (t < ticks) nextMacrotask(tick);
+function measureNativeTap(name: string, action: () => void | Promise<void>): void {
+	void measureNative(name, action, 'native-tap');
+}
+
+const _stormChannel = typeof MessageChannel === 'function' ? new MessageChannel() : null;
+let _stormPending: (() => void) | null = null;
+if (_stormChannel) {
+	_stormChannel.port1.onmessage = () => {
+		const cb = _stormPending;
+		_stormPending = null;
+		if (cb) cb();
@@ -55 +148,27 @@ function runStorm(ticks: number, step: (t: number) => void) {
-	nextMacrotask(tick);
+}
+function nextMacrotask(): Promise<void> {
+	return new Promise((resolve) => {
+		if (!_stormChannel) {
+			setTimeout(resolve, 0);
+			return;
+		}
+		_stormPending = resolve;
+		_stormChannel.port2.postMessage(0);
+	});
+}
+
+function runStorm(ticks: number, step: (t: number) => void): Promise<void> {
+	let tick = 0;
+	const runNext = (): Promise<void> => nextMacrotask()
+		.then(() => {
+			tick++;
+			step(tick);
+			// A separate task is not enough on an asynchronous renderer: without the
+			// acknowledgement, multiple ticks can still coalesce behind one in-flight
+			// commit. Drain each tick before scheduling the next one.
+			return (globalThis as BenchmarkGlobal).__LYNX_BENCH_FLUSH__?.();
+		})
+		.then(() => {
+			if (tick < ticks) return runNext();
+		});
+	return runNext();
@@ -138,2 +257,2 @@ export function App() @{
-	// Storm drivers read ids off a ref so the module-scope tick loop never
-	// captures a stale rows binding (same shape as the React app).
+	// Storm drivers and post-ACK predicates read refs so an async measurement
+	// never observes the render closure from before the acknowledged commit.
@@ -141,0 +261,16 @@ export function App() @{
+	const rowsRef = useRef<RowData[]>(rows);
+	rowsRef.current = rows;
+	const selectedRef = useRef<number | undefined>(selected);
+	selectedRef.current = selected;
+	(globalThis as BenchmarkGlobal).__LYNX_BENCH_SNAPSHOT__ = () => {
+		const current = rowsRef.current;
+		return {
+			rowCount: current.length,
+			firstId: current[0]?.id ?? null,
+			secondId: current[1]?.id ?? null,
+			thirdId: current[2]?.id ?? null,
+			row998Id: current[998]?.id ?? null,
+			firstLabel: current[0]?.label ?? null,
+			selectedId: selectedRef.current ?? null,
+		};
+	};
@@ -143,2 +278 @@ export function App() @{
-	const stormUpdate = useCallback(() => {
-		runStorm(
+	const runUpdateStorm = useCallback(() => runStorm(
@@ -149,2 +283 @@ export function App() @{
-		);
-	}, []);
+		), []);
@@ -152,2 +285 @@ export function App() @{
-	const stormSelect = useCallback(() => {
-		runStorm(STORM_SELECT_TICKS, (t) => {
+	const runSelectStorm = useCallback(() => runStorm(STORM_SELECT_TICKS, (t) => {
@@ -156,2 +288,49 @@ export function App() @{
-		});
-	}, []);
+		}), []);
+	const stormUpdate = useCallback(() => {
+		measureNativeTap('updateStorm', runUpdateStorm);
+	}, [runUpdateStorm]);
+	const stormSelect = useCallback(() => {
+		measureNativeTap('selectStorm', runSelectStorm);
+	}, [runSelectStorm]);
+
+	// DevTool's data-only driver is retained as an explicit diagnostic path. A
+	// publishable run uses real Native taps; result.source makes fallback samples
+	// impossible to mistake for upstream-equivalent input semantics.
+	(globalThis as typeof globalThis & {
+		__LYNX_BENCH_DRIVER__?: { drive(name: string, argument?: number): unknown };
+	}).__LYNX_BENCH_DRIVER__ = {
+		drive(name, argument) {
+			switch (name) {
+				case 'probe': return { ready: true, snapshot: (globalThis as BenchmarkGlobal).__LYNX_BENCH_SNAPSHOT__?.() };
+				case 'snapshot': return (globalThis as BenchmarkGlobal).__LYNX_BENCH_SNAPSHOT__?.();
+				case 'create': {
+					const action = argument === 1000 ? run
+						: argument === 3000 ? run3k
+						: argument === 5000 ? run5k
+						: argument === 10000 ? runLots
+						: argument === 20000 ? run20k
+						: argument === 30000 ? run30k
+						: null;
+					if (action === null) throw new Error(`Unsupported create scale ${argument}.`);
+					return measureNative('create', action, 'devtool-driver');
+				}
+				case 'append1k': return measureNative('append1k', add, 'devtool-driver');
+				case 'update10th': return measureNative('update10th', update, 'devtool-driver');
+				case 'select': {
+					const id = rows[argument ?? -1]?.id;
+					if (id === undefined) throw new Error(`Missing select row ${argument}.`);
+					return measureNative('select', () => select(id), 'devtool-driver');
+				}
+				case 'swap': return measureNative('swap', swapRows, 'devtool-driver');
+				case 'remove': {
+					const id = rows[argument ?? -1]?.id;
+					if (id === undefined) throw new Error(`Missing remove row ${argument}.`);
+					return measureNative('remove', () => remove(id), 'devtool-driver');
+				}
+				case 'clear': return measureNative('clear', clear, 'devtool-driver');
+				case 'updateStorm': return measureNative('updateStorm', runUpdateStorm, 'devtool-driver');
+				case 'selectStorm': return measureNative('selectStorm', runSelectStorm, 'devtool-driver');
+				default: throw new Error(`Unsupported benchmark operation ${name}.`);
+			}
+		},
+	};
@@ -162 +341 @@ export function App() @{
-			<view class="btn" bindtap={run}>
+			<view class="btn" bindtap={() => measureNativeTap('create', run)}>
@@ -165 +344 @@ export function App() @{
-			<view class="btn" bindtap={run3k}>
+			<view class="btn" bindtap={() => measureNativeTap('create', run3k)}>
@@ -168 +347 @@ export function App() @{
-			<view class="btn" bindtap={run5k}>
+			<view class="btn" bindtap={() => measureNativeTap('create', run5k)}>
@@ -171 +350 @@ export function App() @{
-			<view class="btn" bindtap={runLots}>
+			<view class="btn" bindtap={() => measureNativeTap('create', runLots)}>
@@ -174 +353 @@ export function App() @{
-			<view class="btn" bindtap={run20k}>
+			<view class="btn" bindtap={() => measureNativeTap('create', run20k)}>
@@ -177 +356 @@ export function App() @{
-			<view class="btn" bindtap={run30k}>
+			<view class="btn" bindtap={() => measureNativeTap('create', run30k)}>
@@ -180 +359 @@ export function App() @{
-			<view class="btn" bindtap={add}>
+			<view class="btn" bindtap={() => measureNativeTap('append1k', add)}>
@@ -183 +362 @@ export function App() @{
-			<view class="btn" bindtap={update}>
+			<view class="btn" bindtap={() => measureNativeTap('update10th', update)}>
@@ -186 +365 @@ export function App() @{
-			<view class="btn" bindtap={swapRows}>
+			<view class="btn" bindtap={() => measureNativeTap('swap', swapRows)}>
@@ -189 +368 @@ export function App() @{
-			<view class="btn" bindtap={clear}>
+			<view class="btn" bindtap={() => measureNativeTap('clear', clear)}>
@@ -201 +380,6 @@ export function App() @{
-				<Row row={row} isSelected={selected === row.id} onSelect={select} onRemove={remove} />
+				<Row
+					row={row}
+					isSelected={selected === row.id}
+					onSelect={(id) => measureNativeTap('select', () => select(id))}
+					onRemove={(id) => measureNativeTap('remove', () => remove(id))}
+				/>
diff --git a/benchmarks/lynx-table/app/src/app.css b/benchmarks/lynx-table/app/src/app.css
index ebda3de6..0027a39e 100644
--- a/benchmarks/lynx-table/app/src/app.css
+++ b/benchmarks/lynx-table/app/src/app.css
@@ -6,0 +7,7 @@
+.startup-frame-probe {
+	position: absolute;
+	width: 1px;
+	height: 1px;
+	left: -10px;
+	font-size: 1px;
+}
diff --git a/benchmarks/lynx-table/app/src/index.ts b/benchmarks/lynx-table/app/src/index.ts
index cc48ff77..fb3c56a1 100644
--- a/benchmarks/lynx-table/app/src/index.ts
+++ b/benchmarks/lynx-table/app/src/index.ts
@@ -6 +6,38 @@ import './app.css';
-void root.render(App);
+interface StartupObservation {
+	moduleStartMs: number;
+	commitAckMs?: number;
+	firstFrameMs?: number;
+	secondFrameMs?: number;
+}
+
+const benchmarkGlobal = globalThis as typeof globalThis & {
+	__LYNX_BENCH_COMMITTED__?: boolean;
+	__LYNX_BENCH_ERROR__?: string;
+	__LYNX_BENCH_FLUSH__?: () => Promise<void>;
+	__LYNX_BENCH_STARTUP__?: StartupObservation;
+};
+const startup: StartupObservation = { moduleStartMs: Date.now() };
+benchmarkGlobal.__LYNX_BENCH_COMMITTED__ = false;
+benchmarkGlobal.__LYNX_BENCH_FLUSH__ = () => root.flushTransport();
+benchmarkGlobal.__LYNX_BENCH_STARTUP__ = startup;
+const renderResult = root.render(App);
+if (renderResult !== null && typeof renderResult === 'object' && 'then' in renderResult) {
+	void renderResult.then(
+		() => {
+			benchmarkGlobal.__LYNX_BENCH_COMMITTED__ = true;
+			startup.commitAckMs = Date.now();
+			lynx.requestAnimationFrame(() => {
+				startup.firstFrameMs = Date.now();
+				lynx.requestAnimationFrame(() => {
+					startup.secondFrameMs = Date.now();
+					console.log('__NATIVE_BENCH_STARTUP__', JSON.stringify(startup));
+				});
+			});
+		},
+		(error: unknown) => {
+			benchmarkGlobal.__LYNX_BENCH_ERROR__ = error instanceof Error
+				? error.stack ?? error.message
+				: String(error);
+		},
+	);
+}
diff --git a/packages/lynx/src/core/transport.ts b/packages/lynx/src/core/transport.ts
index 6e5f943a..feb91fdd 100644
--- a/packages/lynx/src/core/transport.ts
+++ b/packages/lynx/src/core/transport.ts
@@ -55,0 +56 @@ import type { LynxBackgroundNativeEventDelivery } from './native-event-receiver.
+import { LYNX_DEVELOPMENT } from './environment.js';
@@ -334 +335,6 @@ export function createLynxBackgroundTransport(
-			context.dispatchEvent({ type: LYNX_BACKGROUND_TO_MAIN_EVENT, data: validated });
+			// Production Native ContextProxy delivery is only reliable for strings;
+			// development test hosts preserve the structured-clone contract.
+			context.dispatchEvent({
+				type: LYNX_BACKGROUND_TO_MAIN_EVENT,
+				data: LYNX_DEVELOPMENT ? validated : JSON.stringify(validated),
+			});
@@ -341 +347,4 @@ export function createLynxBackgroundTransport(
-		context.dispatchEvent({ type: LYNX_BACKGROUND_TO_MAIN_EVENT, data: validated });
+		context.dispatchEvent({
+			type: LYNX_BACKGROUND_TO_MAIN_EVENT,
+			data: LYNX_DEVELOPMENT ? validated : JSON.stringify(validated),
+		});
@@ -668 +677,4 @@ export function createLynxBackgroundTransport(
-			context.dispatchEvent({ type: LYNX_BACKGROUND_TO_MAIN_EVENT, data: message });
+			context.dispatchEvent({
+				type: LYNX_BACKGROUND_TO_MAIN_EVENT,
+				data: LYNX_DEVELOPMENT ? message : JSON.stringify(message),
+			});
@@ -1324 +1336,9 @@ export function createLynxBackgroundTransport(
-		if (isRootIndependentDataMessage(event.data)) return;
+		let data: unknown = event.data;
+		if (typeof data === 'string') {
+			try {
+				data = JSON.parse(data);
+			} catch {
+				// The validator below reports a consistently shaped protocol error.
+			}
+		}
+		if (isRootIndependentDataMessage(data)) return;
@@ -1328 +1348 @@ export function createLynxBackgroundTransport(
-			message = validateLynxBackgroundInboundMessage(event.data);
+			message = validateLynxBackgroundInboundMessage(data);
@@ -1331 +1351 @@ export function createLynxBackgroundTransport(
-			rejectExpectedMalformed(event.data, normalized);
+			rejectExpectedMalformed(data, normalized);
@@ -1416,0 +1437,25 @@ export function createLynxBackgroundTransport(
+	// The native main and background scripts initialize independently. Both the
+	// first request and main's unsolicited announcement can legally be emitted
+	// before the peer listener exists, so keep the correlated request alive until
+	// one side confirms readiness. Duplicate request IDs are idempotent on main.
+	let readinessRetryTimer: ReturnType<typeof setTimeout> | null = null;
+	const clearReadinessRetry = (): void => {
+		if (readinessRetryTimer === null) return;
+		clearTimeout(readinessRetryTimer);
+		readinessRetryTimer = null;
+	};
+	const retryReadiness = (): void => {
+		readinessRetryTimer = null;
+		if (readyDeferred.settled || closedError !== null) return;
+		try {
+			dispatch(readinessRequest);
+		} catch (error) {
+			closeInternal(report(error, 'Octane Lynx failed to retry main readiness.'), false);
+			return;
+		}
+		readinessRetryTimer = setTimeout(retryReadiness, 16);
+	};
+	if (!readyDeferred.settled && closedError === null) {
+		readinessRetryTimer = setTimeout(retryReadiness, 16);
+		void readyDeferred.promise.then(clearReadinessRetry, clearReadinessRetry);
+	}
diff --git a/packages/lynx/src/main-thread.ts b/packages/lynx/src/main-thread.ts
index 70e8c28d..3d175415 100644
--- a/packages/lynx/src/main-thread.ts
+++ b/packages/lynx/src/main-thread.ts
@@ -94,0 +95 @@ import { renderLynxFirstScreen, type LynxFirstScreenRenderResult } from './main-
+import { LYNX_DEVELOPMENT } from './core/environment.js';
@@ -658 +659,6 @@ export function installLynxMainThread<Node extends LynxElementRef = LynxElementR
-		context.dispatchEvent({ type: LYNX_MAIN_TO_BACKGROUND_EVENT, data: validated });
+		// Production Native ContextProxy delivery is only reliable for strings;
+		// development test hosts preserve the structured-clone contract.
+		context.dispatchEvent({
+			type: LYNX_MAIN_TO_BACKGROUND_EVENT,
+			data: LYNX_DEVELOPMENT ? validated : JSON.stringify(validated),
+		});
@@ -2266 +2272,4 @@ export function installLynxMainThread<Node extends LynxElementRef = LynxElementR
-									adoption: prepared.firstTreeAction === 'adopt' ? 'adopted' : 'repaired',
+									adoption:
+										prepared.firstTreeAction === 'adopt'
+											? ('adopted' as const)
+											: ('repaired' as const),
@@ -2472,0 +2482,8 @@ export function installLynxMainThread<Node extends LynxElementRef = LynxElementR
+		let data: unknown = event.data;
+		if (typeof data === 'string') {
+			try {
+				data = JSON.parse(data);
+			} catch {
+				// The validator below reports a consistently shaped protocol error.
+			}
+		}
@@ -2476 +2493 @@ export function installLynxMainThread<Node extends LynxElementRef = LynxElementR
-			message = validateLynxBackgroundOutboundMessage(event.data);
+			message = validateLynxBackgroundOutboundMessage(data);
@@ -2480 +2497 @@ export function installLynxMainThread<Node extends LynxElementRef = LynxElementR
-			const identity = recoverIdentity(event.data);
+			const identity = recoverIdentity(data);
@@ -2482,2 +2499,2 @@ export function installLynxMainThread<Node extends LynxElementRef = LynxElementR
-				event.data !== null && typeof event.data === 'object'
-					? (event.data as Record<string, unknown>)
+				data !== null && typeof data === 'object'
+					? (data as Record<string, unknown>)
