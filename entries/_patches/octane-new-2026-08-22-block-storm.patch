diff --git a/benchmarks/lynx-table/app/src/block-program.ts b/benchmarks/lynx-table/app/src/block-program.ts
index 92648fe6c..0e8cc44f9 100644
--- a/benchmarks/lynx-table/app/src/block-program.ts
+++ b/benchmarks/lynx-table/app/src/block-program.ts
@@ -157 +157 @@ stormChannel.port1.onmessage = () => {
-function runStorm(ticks: number, step: (tick: number) => void): void {
+function runStorm(ticks: number, step: (tick: number) => Promise<void>): void {
@@ -161,5 +161,10 @@ function runStorm(ticks: number, step: (tick: number) => void): void {
-		step(tick);
-		if (tick < ticks) {
-			stormPending = next;
-			stormChannel.port2.postMessage(0);
-		}
+		step(tick)
+			.then(() => {
+				if (tick < ticks) {
+					stormPending = next;
+					stormChannel.port2.postMessage(0);
+				}
+			})
+			.catch((error: unknown) => {
+				console.error('[octane-block] storm commit failed', error);
+			});
@@ -249 +254 @@ function reconcile(state: TableState, next: RowData[]): void {
-function commit(state: TableState): void {
+function commit(state: TableState): Promise<void> {
@@ -256,0 +262 @@ function commit(state: TableState): void {
+	return state.committing;
@@ -295 +301 @@ function updateTenth(state: TableState): void {
-function select(state: TableState, id: number): void {
+function select(state: TableState, id: number): Promise<void> {
@@ -297 +303 @@ function select(state: TableState, id: number): void {
-	if (previous === id) return;
+	if (previous === id) return state.committing;
@@ -307 +313 @@ function select(state: TableState, id: number): void {
-	commit(state);
+	return commit(state);
@@ -352 +358 @@ function updateStorm(state: TableState): void {
-		commit(state);
+		return commit(state);
@@ -360,0 +367 @@ function selectStorm(state: TableState): void {
+		return state.committing;
