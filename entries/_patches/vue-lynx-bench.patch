[33mdiff --git a/packages/benchmark/apps/ui-react/src/App.tsx b/packages/benchmark/apps/ui-react/src/App.tsx[m
[33mindex 06d5ca88..3341335b 100644[m
[33m--- a/packages/benchmark/apps/ui-react/src/App.tsx[m
[33m+++ b/packages/benchmark/apps/ui-react/src/App.tsx[m
[35m@@ -4,11 +4,17 @@[m
 // immutable state updates + memoized row component.[m
 import { memo, useCallback, useEffect, useRef, useState } from '@lynx-js/react';[m
 [m
[31m-import { buildData } from './data';[m
[32m+[m[32mimport { buildData, buildDataSeeded } from './data';[m
 import type { RowData } from './data';[m
 [m
 import './App.css';[m
 [m
[32m+[m[32m// Mount-create ladder: __BENCH_AUTOROWS__ = N builds a variant whose table is[m
[32m+[m[32m// already populated at first render. Mirrors the Vue and Octane apps.[m
[32m+[m[32mdeclare const __BENCH_AUTOROWS__: number;[m
[32m+[m[32mconst INITIAL_ROWS: RowData[] =[m
[32m+[m[32m  __BENCH_AUTOROWS__ > 0 ? buildDataSeeded(__BENCH_AUTOROWS__) : [];[m
[32m+[m
 // -- storms: N sequential state→render→DOM ticks from one click --------------[m
 // Each tick runs in its own macrotask (MessageChannel avoids the nested[m
 // setTimeout 4ms clamp) so every mutation goes through a full render cycle[m
[35m@@ -62,7 +68,7 @@[m [mconst Row = memo(function Row({ row, isSelected, onSelect, onRemove }: RowProps)[m
 });[m
 [m
 export function App() {[m
[31m-  const [rows, setRows] = useState<RowData[]>([]);[m
[32m+[m[32m  const [rows, setRows] = useState<RowData[]>(INITIAL_ROWS);[m
   const [selected, setSelected] = useState<number | undefined>(undefined);[m
 [m
   const run = useCallback(() => {[m
[33mdiff --git a/packages/benchmark/apps/ui-react/src/data.ts b/packages/benchmark/apps/ui-react/src/data.ts[m
[33mindex 2e220167..052b0f54 100644[m
[33m--- a/packages/benchmark/apps/ui-react/src/data.ts[m
[33m+++ b/packages/benchmark/apps/ui-react/src/data.ts[m
[35m@@ -41,3 +41,29 @@[m [mexport function buildData(count = 1000): RowData[] {[m
   }[m
   return data;[m
 }[m
[32m+[m
[32m+[m[32m// Deterministic variant for the mount-create (autoRows) ladder; see[m
[32m+[m[32m// ../../shared/data.ts for rationale. mulberry32(42).[m
[32m+[m[32mexport function buildDataSeeded(count: number): RowData[] {[m
[32m+[m[32m  let seed = 42 >>> 0;[m
[32m+[m[32m  const rand = () => {[m
[32m+[m[32m    seed = (seed + 0x6d2b79f5) >>> 0;[m
[32m+[m[32m    let t = seed;[m
[32m+[m[32m    t = Math.imul(t ^ (t >>> 15), t | 1);[m
[32m+[m[32m    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);[m
[32m+[m[32m    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;[m
[32m+[m[32m  };[m
[32m+[m[32m  const pick = (max: number) => Math.round(rand() * 1000) % max;[m
[32m+[m[32m  const data: RowData[] = [];[m
[32m+[m[32m  for (let i = 0; i < count; i++) {[m
[32m+[m[32m    data.push({[m
[32m+[m[32m      id: ID++,[m
[32m+[m[32m      label: adjectives[pick(adjectives.length)][m
[32m+[m[32m        + ' '[m
[32m+[m[32m        + colours[pick(colours.length)][m
[32m+[m[32m        + ' '[m
[32m+[m[32m        + nouns[pick(nouns.length)],[m
[32m+[m[32m    });[m
[32m+[m[32m  }[m
[32m+[m[32m  return data;[m
[32m+[m[32m}[m
[33mdiff --git a/packages/benchmark/apps/ui-vapor/src/App.vue b/packages/benchmark/apps/ui-vapor/src/App.vue[m
[33mindex bf99c553..ee51daf9 100644[m
[33m--- a/packages/benchmark/apps/ui-vapor/src/App.vue[m
[33m+++ b/packages/benchmark/apps/ui-vapor/src/App.vue[m
[35m@@ -8,13 +8,20 @@[m
 // by hand. The React variant (apps/ui-react) mirrors these operations with[m
 // idiomatic React state.[m
 import { ref, shallowRef, triggerRef } from 'vue'[m
[31m-import { buildData } from '../../../shared/data'[m
[32m+[m[32mimport { buildData, buildDataSeeded } from '../../../shared/data'[m
 import type { RowData } from '../../../shared/data'[m
 [m
 const MODE = __BENCH_MODE__[m
 [m
[32m+[m[32m// Mount-create ladder: __BENCH_AUTOROWS__ = N builds a variant whose table is[m
[32m+[m[32m// already populated at first render, so startup cost at scale is measurable[m
[32m+[m[32m// without taps (and IFR-capable configs exercise their first-frame path).[m
[32m+[m[32mdeclare const __BENCH_AUTOROWS__: number[m
[32m+[m
 const selected = shallowRef<number | undefined>(undefined)[m
[31m-const rows = shallowRef<RowData[]>([])[m
[32m+[m[32mconst rows = shallowRef<RowData[]>([m
[32m+[m[32m  __BENCH_AUTOROWS__ > 0 ? buildDataSeeded(__BENCH_AUTOROWS__) : [],[m
[32m+[m[32m)[m
 const ready = ref('ready')[m
 [m
 function run() {[m
[33mdiff --git a/packages/benchmark/apps/ui-vdom/src/App.vue b/packages/benchmark/apps/ui-vdom/src/App.vue[m
[33mindex 84dd2197..d12f2b17 100644[m
[33m--- a/packages/benchmark/apps/ui-vdom/src/App.vue[m
[33m+++ b/packages/benchmark/apps/ui-vdom/src/App.vue[m
[35m@@ -7,13 +7,20 @@[m
 // by hand. The React variant (apps/ui-react) mirrors these operations with[m
 // idiomatic React state.[m
 import { ref, shallowRef, triggerRef } from 'vue'[m
[31m-import { buildData } from '../../../shared/data'[m
[32m+[m[32mimport { buildData, buildDataSeeded } from '../../../shared/data'[m
 import type { RowData } from '../../../shared/data'[m
 [m
 const MODE = __BENCH_MODE__[m
 [m
[32m+[m[32m// Mount-create ladder: __BENCH_AUTOROWS__ = N builds a variant whose table is[m
[32m+[m[32m// already populated at first render, so startup cost at scale is measurable[m
[32m+[m[32m// without taps (and IFR-capable configs exercise their first-frame path).[m
[32m+[m[32mdeclare const __BENCH_AUTOROWS__: number[m
[32m+[m
 const selected = shallowRef<number | undefined>(undefined)[m
[31m-const rows = shallowRef<RowData[]>([])[m
[32m+[m[32mconst rows = shallowRef<RowData[]>([m
[32m+[m[32m  __BENCH_AUTOROWS__ > 0 ? buildDataSeeded(__BENCH_AUTOROWS__) : [],[m
[32m+[m[32m)[m
 const ready = ref('ready')[m
 [m
 function run() {[m
[33mdiff --git a/packages/benchmark/shared/data.ts b/packages/benchmark/shared/data.ts[m
[33mindex cba68955..1ab39c90 100644[m
[33m--- a/packages/benchmark/shared/data.ts[m
[33m+++ b/packages/benchmark/shared/data.ts[m
[35m@@ -46,3 +46,32 @@[m [mexport function buildData(count = 1000): RowData[] {[m
   }[m
   return data;[m
 }[m
[32m+[m
[32m+[m[32m// Deterministic variant for the mount-create (autoRows) ladder: labels must be[m
[32m+[m[32m// byte-identical across builds and frameworks so first-screen bundles and wire[m
[32m+[m[32m// traffic stay comparable. mulberry32(42), same convention as the Octane app.[m
[32m+[m[32mexport function buildDataSeeded(count: number): RowData[] {[m
[32m+[m[32m  let seed = 42 >>> 0;[m
[32m+[m[32m  const rand = () => {[m
[32m+[m[32m    seed = (seed + 0x6d2b79f5) >>> 0;[m
[32m+[m[32m    let t = seed;[m
[32m+[m[32m    t = Math.imul(t ^ (t >>> 15), t | 1);[m
[32m+[m[32m    t ^= t + Math.imul(t ^ (t >>> 7), t | 61);[m
[32m+[m[32m    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;[m
[32m+[m[32m  };[m
[32m+[m[32m  const pick = (max: number) => Math.round(rand() * 1000) % max;[m
[32m+[m[32m  const data: RowData[] = [];[m
[32m+[m[32m  for (let i = 0; i < count; i++) {[m
[32m+[m[32m    data.push({[m
[32m+[m[32m      id: ID++,[m
[32m+[m[32m      label: shallowRef([m
[32m+[m[32m        adjectives[pick(adjectives.length)][m
[32m+[m[32m          + ' '[m
[32m+[m[32m          + colours[pick(colours.length)][m
[32m+[m[32m          + ' '[m
[32m+[m[32m          + nouns[pick(nouns.length)],[m
[32m+[m[32m      ),[m
[32m+[m[32m    });[m
[32m+[m[32m  }[m
[32m+[m[32m  return data;[m
[32m+[m[32m}[m
